# Providers

This example workspace is dry-run only.

Model labels are user-owned. V1 renders receipts but does not call a provider.

- claude: use the client environment
- codex: use the client environment
- gemini: use the client environment
